id: force_lte_only
name: "Force Phone to LTE (4G)"
entry_point: "modules.device_actions:force_lte_only"
timeout_sec: 45
inputs: {}
outputs:
  - key: success
    type: boolean
  - key: note
    type: string
version: "1.0.0"

