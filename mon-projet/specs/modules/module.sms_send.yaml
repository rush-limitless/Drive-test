id: sms_send
name: "Send SMS"
entry_point: "modules.device_actions:send_sms"
timeout_sec: 90
inputs:
  recipient: {type: string, required: true}
  message: {type: string, required: true}
outputs:
  - key: recipient
    type: string
version: "1.0.0"

