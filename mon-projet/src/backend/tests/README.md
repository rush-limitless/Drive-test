## Tests automatisés

Les tests FastAPI peuvent être lancés directement depuis la racine `mon-projet/src/backend` :

```powershell
cd C:\Users\rush\Pictures\ADB Tool\ADB-automation-tool\mon-projet\src\backend
..\..\.\.venv\Scripts\python.exe -m pytest
```

Pour cibler un fichier précis (ex : santé API) :

```powershell
..\..\.\.venv\Scripts\python.exe -m pytest tests\test_health_api.py -vv
```

Les tests utilisent `TestClient` et ne nécessitent pas de périphériques physiques, mais ils attendent que les dépendances Python soient installées (`pip install -r requirements.txt`).*** End Patch*** End Patch
```}
>]
``` woont wrong? Need proper apply_patch. Need escapes? structure: use apply_patch simple. Let's reapply carefully. Need to include newline at end. use earlier command. We'll re-run patch. Continuous. Use plain text no JSON. workaround. We'll re-run? previous command invalid? there is weird string include? scrap. We'll send proper command now. continuous. Let's create file with apply patch proper. continuous input. continuous string. need to ensure no lumps. We'll re-run. continuous. after pyro handshake? we need to send actual patch. continuous. Use multi-line string? done. Let's attempt: continuous call apply_patch with impetus; include actual patch. Continuous. end. Let's redo. continuous. continuous. continuous. continuous. continuous. continuous. continuous. continuous. continuous. continuous. continuous. continuous. continuous. continuous. continuous. continuous. for autop. continuous - call apply_patch. continuous. continuous. continuous. continuous. continuous. continuous. continuous. continuous. continuous. continuous. continuous. continuous. continuous. continuous. continuous. ok.
```
